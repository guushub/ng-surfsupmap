import { DataRegionDirective } from './data-region.directive';

describe('DataRegionDirective', () => {
  it('should create an instance', () => {
    const directive = new DataRegionDirective();
    expect(directive).toBeTruthy();
  });
});
